# docs/设计想法 —— 设计稿与探询记录(历史存档,只读;仅 `日志/` 子目录为现行维护文档)

> 这里放**动手前的设计过程**:主题/视觉方案、首页组件重构探询、游戏规划设计等。
> 性质是**设计过程存档,不是实现事实**。实现事实以 `frontend/src` + `backend/src/main/java` 为准,功能现状见 `docs/需求设计说明书.md`,落地过程与踩坑见 `docs/变更归档.md`。
> 目录于 2026-09-17 重组(原先散在 `docs/` 根下的 `首页组件重构-*.md`、`光尘主题/*` 等一并迁入),文件内的引用路径已同步修正。此后**只读不动**:新设计稿请新建子目录,不要改写这里已归档的结论。
> **⚠ 例外:`日志/` 子目录不是历史草稿**。它放**现行规范**——《日志规范》(开发强制规范,`AGENTS.md` 后端规范 14 指向它)与《日志问题分析方法》(运维/业务排查方法论),两份都要**跟着实现更新**;上面「只读不动」只约束设计稿部分。两文件于 2026-09-26 从 `docs/` 根迁入此处,同步修正了全库引用。

## 一、内容与状态

| 子目录 / 文件 | 内容 | 落地状态 | 现行实现 |
|---|---|---|---|
| `主题/暖居主题/设计理念.md` | **设计灵魂文档(North Star)**:「会呼吸的家」的情感与审美方向,自述为所有未来设计的分析基石,用户每次表达的感受累积于此 | 长期有效(**本目录唯一按设计应继续累积的一份**) | — |
| `主题/暖居主题/主题系统设计.md` | 主题 × 晨暮 两轴矩阵 + 旗舰「暖居」的跨会话交接底稿 | 已落地 | V9.60–V9.66(见需求设计说明书 §4.10.2 主题) |
| `主题/暖居主题/暖居-视觉解析.md`、`暖居-晨暮预览.html`、`preview-dawn.png`/`preview-dusk.png` | 暖居调色板与组件规格、晨暮两态静态预览 | 已落地(预览是静态对照,可随时浏览器直接打开) | `frontend/src/styles/main.css` 的 `html.theme-warm` 段 |
| `主题/光尘主题/首页组件/首页组件重构-排版方案.md`(结论)、`首页组件重构-设计探询记录.md`(问答过程存档) | 首页组件重构:四档 snap + hover 推开邻居 + 内容渐进展开 | 已落地 | V9.53 / V9.66(见 §4.7.7 首页仪表盘) |
| `主题/光尘主题/首页组件/光尘首页hover放大逻辑备份.md` | 被移除的「P2 hover 放大 + 推开邻居」**原样代码备份**(留作恢复参照) | 代码已移除(V9.65),备份有意保留 | — |
| `主题/光尘主题/相册封面/album-cover-v1~v3.jpeg`(4 张) | 相册封面设计原稿 | 已落地(v3 压缩后作为运行时资产) | `frontend/src/assets/album-cover.jpg`(V9.54) |
| `功能/小游戏/植物养殖游戏设计.md` | 花园植物养殖(V1 设计稿) | 已落地 | V9.68(见 §4.13.1) |
| `主题/场景主题/设计对话记录.md` + `preview/`(切图与对比稿) | 场景主题的**唯一事实源与跨会话锚点**,含大量生图迭代与抠图 | **未落地,方向已撤**(V9.60 移除 2D 半成品;未来场景主题改以 3D `/tools/light-lab` 为底座) | 无 |
| `日志/日志规范.md` | **现行强制规范**(非草稿):三类日志文件、六要素、tid 贯穿、级别标准、三方走 `ThirdPartyHttp`、堆栈写法、脱敏清单、保留策略 | **长期有效**,随日志实现同步更新 | `AGENTS.md` 后端规范 14 指向它;实现见 `docs/需求设计说明书.md` §4.6.8 |
| `日志/日志问题分析方法.md` | **现行排查方法论**(非草稿):拿 tid 四途径 → 详细日志页 → 四步分析 + 案例 + grep 命令 + 速查表 | **长期有效**,面向运维/业务人员 | 运维「详细日志」页 `GET /ops/logs/trace?tid=`(§4.6.8) |

## 二、⚠ 仓库体积提醒

本目录现约 **79MB**,且**全部已随仓库提交**(2026-09-26 核对:`git ls-files docs/设计想法` = 72 个文件,含本轮从 `docs/` 根迁入的 `日志/` 两份 md)。其中 `主题/场景主题/preview/` 一项就约 **73.6MB**(单文件最大 8.4MB),而 `仓库 .git` 已达 **120MB**。

这与「位图只入库压缩后的运行时资产」的既有原则冲突(见 `docs/变更归档.md` 首页相册封面小节)。**清理候选清单**见 `docs/变更归档.md` 的「文档治理(2026-09-26)」小节——本目录的图片类草稿在候选之列,是否移出仓库待决定;`设计理念.md` / `主题系统设计.md` 等**文档类**不受影响。

## 三、检索提示

- 找「某个视觉/交互当初为什么这么设计」→ 在本目录 grep 关键词。
- 找「现在是什么样」→ 不要看这里,读 `docs/需求设计说明书.md` 对应 §4 小节。
- 找「当初怎么改的、踩了什么坑」→ `docs/变更归档.md`。
