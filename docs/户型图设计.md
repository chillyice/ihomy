# 物品定位 · 户型图功能设计(计划 v4)

> 状态:**S1~S5 全部完成 + 房间裁剪/粘合已实现**(开发分支 `item_seeker`;S1+S2 已合 main,S3 之后待合)。实现过程中的设计变更与迭代记录见文末「实现记录」§14(S1+S2)与 §15(S3~S5+迭代+裁剪/粘合)。
> 本文是「物品定位-户型图」(P2 待办)的完整设计,所有决策经与用户逐条确认后定稿。

## 0. 目标与边界

家庭内「找东西」的**示意级**户型图:正交多边形房间 + 矩形家具 + 物品点,三层同一张画布,可拖拽编辑、可吸附、可标定比例、可数值输入精确尺寸。

**页面 = 户型图主视图(不是 tab)**:物品定位页整个主体就是画布,不加独立导航。顶部常驻搜索框 + 房子/楼层切换 + 「列表/编辑」按钮;空态时画布中央大「+」提示加房子。

**两模式分离(核心)**:「查看/找东西模式」与「编辑模式」严格分开。
- 查看/找东西(默认):顶部搜索框 → 下方户型图标注物品位置,形成「搜东西 → 看在哪」闭环。
- 编辑:点「编辑」才进入,画房间/摆家具/放物品/上传底图/标定;点「完成」退出。

**徒手画 = 一等公民**:底图只是可选辅助。不是人人都有户型图,没有底图也要画得舒服。

**编辑器不做半成品**:户型图一次画好长期用,但家具位置会变、可能要画多张图;编辑能力必须完整好用,不能偷懒。

**不做**:斜墙/圆弧墙、共享墙实体、墙厚建模、门窗符号、内置家具真实尺寸库(逐件录入真实尺寸可以)、CAD(DXF/DWG)导入、建筑规范级面积核算、多人实时同屏。

## 1. 数据模型(定死)

### 1.1 房间 = 正交多边形(替换矩形)

`family_room` 删除 `x/y/w/h`,新增 `geometry TEXT`(JSON 有序顶点数组):

```json
[{"x":0,"y":0},{"x":300,"y":0},{"x":300,"y":200},
 {"x":100,"y":200},{"x":100,"y":400},{"x":0,"y":400}]
```

- 边只允许横/竖(正交),矩形 = 4 个顶点,是特例。
- 顶点 ≥ 4,闭合;缺口/L 形/T 形都是同一份多边形数据,无「缺口列表」补丁结构。

### 1.2 家具 = 矩形 + 类型 + 自定义命名 + 家具库

`family_furniture` 新增:
- `x / y / w / h`(DECIMAL,可空 = 尚未摆上户型图)。
- `type`(VARCHAR,预设类型清单 + 可自定义):衣柜/床/冰箱/书桌/沙发/茶几/柜子/餐桌/书架/其他。类型给图标。
- `name`(已有,自定义命名,如「主卧衣柜」「床1」):区分同种家具,自由填。
- `room_id` 可空 = 家具在「家具库」(未分配房间)。**删房间时家具进库(room_id 置空)、与物品的绑定(`furniture_id`)不动**,新房间建好后从库拖回。圆形家具不做。

### 1.3 物品 = 锚点 + 相对坐标 + 位置描述(无抽屉层级)

`family_item`:
- `furniture_id` 非空 → 物品锚在**家具**上,`rel_x/rel_y` 为家具包围盒内 0~1 相对位置。
- `furniture_id` 空(散放)→ 物品锚在**房间**上,`rel_x/rel_y` 为房间包围盒内 0~1 相对位置。
- `position`(文本,已有)承载精确定位描述,如「第三层左侧抽屉」——定位到某家具后,一个文本字段描述即可,**不建抽屉/隔层层级**。

### 1.4 墙 = 房间之间的空隙,无实体

墙厚 = 两房间多边形之间的空隙;不建墙实体,不量厚度。

### 1.5 楼层配置(底图 + 比例尺按楼层,默认 100px/m)

`family_house` 删除单值 `plan_image_url / plan_scale`,新增 `floor_plans TEXT`(JSON,key=楼层号):

```json
{"1":{"imageUrl":"/files/x.jpg","scale":100.0},"2":{"imageUrl":"/files/y.jpg","scale":100.0}}
```

- 楼层列表 = `floor_plans` 的 key ∪ `room.floor`;「加楼层」= 加一个 key。
- 每楼层独立底图 + 比例尺;楼层 label 由数字派生(`1`→「1楼」,`-1`→「地下1层」)。
- **默认 `scale = 100px/m`**(非空):160㎡ 房子 ≈ 13m ≈ 1300px,加载时自动缩放到一屏。
- **标定 = 可选校正**:有底图时两点测距校正 scale;无底图直接用默认值画,不是前置必须。
- 不加 `family_floor` 表(避免表数膨胀);将来需要楼层名/排序时再升级为表。

## 2. 坐标系

- 每「房子 × 楼层」一张画布,单一像素坐标系。
- 三层同坐标系:房间(多边形)、家具(矩形)、物品(点,由锚点换算成绝对坐标)。
- 底图与比例尺按楼层存(`family_house.floor_plans`),底图铺在原点;`scale`(像素/米)默认 `100px/m`,标定后校正,用于尺寸显示。

## 3. 家具随房间移动规则(用户确认)

- **房间整体平移** → 家具跟随(级联平移)。
- **房间变形(拖边/角)** → 家具原地不动、不缩放、不变形。
- 例:靠墙橱柜,后墙被移动时,橱柜不动,只有墙变位置。
- 实现:家具存画布绝对坐标;房间平移时对归属该房间的家具级联平移同 delta,变形时不做任何级联。

## 4. 渲染:SVG,手绘草图风

- 房间 = `<polygon>`,家具 = `<rect>`,物品 = `<g>`。
- **视觉 = 手绘草图风**(铅笔线条感),契合现有暖棕毛玻璃主题。
- 选型理由:房间模型即正交多边形,SVG 的 polygon 与之 1:1;顶点手柄、边中点手柄、点选判定、吸附距离、尺寸线全内置;对象数量(几十个)远低于 canvas 优势阈值。
- 不用 div+clip-path(缺口多边形丑、点选手写)、不用 canvas(点选/文字/样式全手写)、不引 fabric/Konva(多边形顶点编辑是弱项,加依赖)。

## 5. 模式与页面布局(用户确认)

### 5.1 两种模式

- **查看/找东西模式(默认)**:顶部搜索框 + 户型图主体;搜物品 → 画布放大高亮位置。
- **编辑模式**:点「编辑」进入,左侧出酷家乐式侧栏,画房间/摆家具/放物品/上传底图/标定;点「完成」退出。
- 切换显式,避免查看时误拖。

### 5.2 桌面页面布局

```
┌──────────────────────────────────────────────────────────────┐
│  [房子▾]          [🔍 搜索物品名称……]            [列表] [编辑] │
├──────────────────────────────────────────────────────────────┤
│  ┌─────────┐                                                 │
│  │ 编辑侧栏 │         户型图画布(SVG, 手绘草图风)              │
│  │ 房间     │                                                 │
│  │ 家具     │                                ┌─────────────┐ │
│  │ 库       │                                │ 搜索命中     │ │
│  └─────────┘                                │ 剪子 ×3     │ │
│                                             │✓书房书桌…   │ │
│  ┌─────┐                                    │ 主卧梳妆…   │ │
│  │ 3F  │ ← 楼层切换(左下角,商厦模式)          │ 次卧柜子…  │ │
│  │ 2F  │                                    └─────────────┘ │
│  │ 1F  │                                                   │
│  └─────┘                                                   │
└──────────────────────────────────────────────────────────────┘
```

- **顶栏常驻**:房子下拉(6 套房主切换)+ 搜索框 + 「列表」「编辑」按钮。
- **左下角楼层切换器**:竖排 `1F/2F/3F`,当前层高亮(商厦电梯地图惯例);**没画户型图的房子不显示、默认 1F**。
- **搜索命中**:右下角弹结果列表(所有命中,按房子/楼层/位置分组),当前画布展示的那几把在列表里显著加亮;点某条 → 自动切房/楼层 + 画布高亮。
- **编辑态左侧侧栏(酷家乐式)**:子 tab「房间 / 家具 / 库」,拖拽/点击素材到画布;**查看态隐藏**。
- **空状态**:无房子时画布中央大「+」→ 建房子。
- **查看态(默认)**:每次进来直接看户型图;物品标记**不常显**,搜索命中或点家具才浮现。
- **列表**:切到旧方式房子/房间/家具/物品 CRUD。

### 5.3 加数据的两种方式(并存)

- **旧方式(列表)**:点「列表」→ 房子/房间/家具/物品 CRUD,不画户型图也能用。
- **画布方式(编辑态)**:画完/摆完弹窗命名,一步建好实体 + 几何;或从编辑侧栏(房间/家具/库)拖入画布。
- **家具库**:删房间后家具进库(room_id 空),从编辑侧栏「库」子 tab 拖回新房间。

### 5.4 移动端

- **只读户型图** + 搜索定位(能看、能找)。
- **可增删改房子/房间/家具/物品**(列表 CRUD)。
- **不做画布编辑**(编辑户型图桌面专属)。

## 6. 编辑手势(9 个必须 + 2 个创建/定位,仅编辑态生效)

| 手势 | 效果 |
|------|------|
| 拖房间主体 | 全顶点平移 |
| 拖房间顶点 | 移动单个角(四角、缺口角通用) |
| 拖房间边中点 | 整条边平移(相邻两点跟随,保持直角) |
| 双击房间边 | 插入新顶点(造缺口) |
| 双击房间顶点 | 删除该顶点(去缺口,最少 4 点) |
| 拖家具主体 | 移动家具 |
| 拖家具边/角 | 缩放家具 |
| 拖物品标记 | 锚点内定位 |
| 空白拖拽/滚轮 | 平移/缩放画布 |
| 拖拽拉矩形 / 点击逐点描绘 | 画房间(逐点描绘双击闭合,直接画 L 形/缺口) |
| 定位模式 | 物品搜索/列表「定位」跳转画布,放大居中高亮该物品 |

## 7. 吸附规则

- 不建共享墙,两房间独立;拖靠拢一侧的边只动被拖房间,邻居不动。
- 磁吸:边对边、顶点对顶点,阈值 ~6px,吸附时显示对齐虚线提示。
- 家具同理:家具边吸附房间边/其他家具边(贴墙摆放柜子)。

## 8. 尺寸标注与数值输入(用户确认)

- **边长常显开关**:开 = 所有边常显米数;关 = 拖动时只显示「受影响的边」。
  - 拖角 → 显示该角的两条边。
  - 拖边 → 显示该边的两条相邻边(拖边本身长度不变,变的是相邻边)。
- **数值输入**:选中边/角,键入精确米数(精确到毫米 = 3 位小数,如 `3.250`),程序换算像素。
- 拖拽 = 大概定型,数值输入 = 雕刻。
- 默认 `100px/m` 即显示米数;标定后更准,不是「未标定不显示」。

## 9. 底图标定与房间面积

- **底图**(可选辅助,按楼层):上传当前楼层户型图图片铺作底图,两点测距输入实际米数 → 校正该楼层 `scale`。
- **房间面积**(附带):多边形面积公式(shoelace),显示 `xx.xx m²`。

## 10. 后端接口

| 接口 | 说明 |
|------|------|
| `GET /item/floor-plan?houseId=&floor=` | 一次返回 house 元信息(该楼层底图/比例尺)+ rooms(几何)+ furnitures(几何+type)+ items(锚点/相对坐标/position) |
| `PUT /item/house/{id}/floor-plans` | 保存楼层配置 JSON(底图上传/移除、比例尺校正、加楼层) |
| `PUT /item/room/{id}/geometry` | 保存房间多边形(替代原 `room/{id}/plan`) |
| `PUT /item/furniture/{id}/geometry` | 保存家具矩形 + type(新增) |
| `PUT /item/{id}/place` | 物品相对坐标(保留) |
| 房/家具/物品 CRUD | 保留,仅增字段 |
| `GET /item/furniture?roomId=`(空) | 家具库列表(编辑侧栏「库」子 tab);`room_id` 空 = 在库 |

> 搜索无独立接口:走 `GET /item/list`(全字段)+ 前端按名称过滤;命中项的 `furniture_id → room → floor` 联动 `floor-plan` 定位高亮。

## 11. 分期(用户授权由我定)

- **S1 数据模型重构**:schema + 后端全改 + migration(存量 `x/y/w/h` → 4 点多边形;`plan_image_url/plan_scale` → `floor_plans` JSON;家具加 `type`)。这一关过了才不屎山。
- **S2 页面骨架 + 编辑手势**:主视图 + 查看/编辑两模式 + 空态「+」+ 搜索框 + 列表入口 + 左下角楼层切换器 + 编辑侧栏(房间/家具/库);SVG 渲染 + 顶点/边/主体拖动 + 双击加点/删点 + 家具拖拽缩放 + 逐点描绘。
- **S3 吸附**:边/顶点磁吸 + 家具吸附 + 对齐虚线。
- **S4 墙内柜 + 尺寸标注 + 底图标定 + 面积 + 定位模式 + 手绘风视觉**:家具跨房间边界;边长常显开关 + 数值输入;按楼层标定(校正默认 scale);面积显示;搜索命中结果列表(角落,跨房子/楼层分组)+ 物品「定位」跳转高亮;移动端只读户型图 + 列表 CRUD。
- **S5 打磨**:撤销、重叠提示、乐观锁、小地图、移动端 pinch、空状态引导、网格吸附、复制房间。

## 12. 对当前分支已建代码的迁移

| 现在 | 改成 |
|------|------|
| `family_room.x/y/w/h` | `family_room.geometry` 多边形 |
| `family_house.plan_image_url / plan_scale` | `family_house.floor_plans` JSON(按楼层) |
| `family_item.rel_x/rel_y` | 保留,语义改为相对锚点包围盒 |
| `family_furniture`(无 type) | 加 `type`(预设+自定义);`room_id` 改可空(空=家具库) |
| 房间 = div 矩形 | 房间 = SVG polygon |
| 户型图 = 一个 tab | 户型图 = 页面主视图 |
| `PUT /room/{id}/plan` | `PUT /room/{id}/geometry` |
| 物品相对房间 | 相对锚点(家具优先,散放才是房间) |

迁移脚本把存量矩形房间回填成 4 点多边形(测试库几乎无存量坐标,风险近零)。

## 13. 决策记录(与用户确认,冻结)

| # | 决策 |
|---|------|
| 1 | 粒度:房间多边形 / 家具矩形 / 物品点 |
| 2 | 不建共享墙,靠吸附对齐 |
| 3 | 家具上画布、绑房间、随房间平移、不随变形 |
| 4 | 渲染用 SVG |
| 5 | 分期 S1→S5 由开发者定 |
| 6 | 工作模式:问→答→调整记录,成熟前不开工 |
| 7 | 尺寸标注:米,精确到毫米,常显开关 + 数值输入 |
| 8 | 底图要做(可选辅助);房间面积为附带功能 |
| 9 | 画布为主入口,CRUD 降级为列表/查找/管理 |
| 10 | 楼层配置按楼层存(`family_house.floor_plans` JSON,不加新表) |
| 11 | 画房间两种方式:拖矩形 + 点击逐点描绘(双击闭合) |
| 12 | 定位模式:物品「定位」跳转画布放大高亮 |
| 13 | 页面 = 户型图主视图(非 tab),不加独立导航;顶部搜索框 |
| 14 | 两模式分离:查看/找东西(默认)与 编辑 分开,点「编辑」进入 |
| 15 | 空态画布中央大「+」加房子;编辑完每次进来直接看到户型图 |
| 16 | 加数据两种方式并存:旧列表 CRUD + 画布内创建 |
| 17 | 全家都能改,无编辑权限分层 |
| 18 | 徒手画 = 一等公民,底图只是可选辅助 |
| 19 | 边强制横竖(正交) |
| 20 | 默认比例尺 100px/m(非空),标定降级为可选校正 |
| 21 | 精度毫米(3 位小数) |
| 22 | 家具需真实尺寸 + `type`(预设+自定义)+ 自定义 `name` |
| 23 | 物品无抽屉层级,`position` 文本字段承载位置描述 |
| 24 | 视觉 = 手绘草图风 |
| 25 | 移动端只读户型图 + 列表 CRUD,无画布编辑 |
| 26 | 搜索多结果 → 角落结果列表(跨房子/楼层/位置分组,当前层命中显著)+ 画布高亮当前层全部命中;语音反馈=未来愿景 |
| 27 | 物品标记不常显,搜索命中或点家具才浮现 |
| 28 | 一套房多层、每层一张;楼层切换器左下角(商厦电梯模式),没画图的房子不显示、默认 1F |
| 29 | 删房间 → 家具进「家具库」(room_id 空),与物品绑定不动,新房间拖回 |
| 30 | 房间命名 = 预设 + 自定义(同家具 type 思路) |
| 31 | 编辑态左侧 = 酷家乐式侧栏(房间/家具/库子 tab),查看态隐藏 |

## 14. 实现记录(2026-09-03,S1+S2 已完成)

> 以下为实际实现与原设计(v4)的差异,以功能为准。实现时用户提出三条需求调整,均已在代码落实。

### 14.1 允许斜边(替代决策 #19「边强制横竖」)

- **去掉强制直角**:顶点拖动不再自动调整相邻顶点,顶点自由跟随鼠标;边拖动不再锁横/竖,两端点一起自由平移。多边形可出现斜边。
- **改为轴对齐吸附(辅助,非强制)**:拖动顶点时,当某条邻边贴近横轴(该顶点 y 接近 prev.y 或 next.y)或纵轴(x 接近 prev.x 或 next.x)时,吸附对齐到对应轴,辅助把边/角调成矩形;阈值 6px;吸附瞬间顶点手柄变绿(`.fp-handle.is-snapped`)。
- 保留:拖房间主体=全顶点平移;双击顶点=删点(最少 4 点);拖家具=移动、拖角=缩放;拖物品=定位;空白拖拽/滚轮=平移缩放画布。

### 14.2 边加点方式(替代「双击边插入顶点」)

- 设计原为「双击边插入顶点(两阶段折边造缺口)」,实现改为**「hover 边 + 点击插入端点」**:编辑态鼠标靠近边(阈值 12px)时,在边投影点显示一个圆圈包裹的加号,随鼠标沿边移动;点击后在投影点插入端点 d,原边 ab 变为 ad + db(顶点数组在 a、b 之间 splice 插入)。
- 鼠标靠近顶点手柄或边中点手柄(12px 内)时不显示加号,避免误触拖拽手柄。
- 简单加点,不再做折边(凸起/凹陷)状态机。

### 14.3 其余 S2 实现要点

- **页面 = 户型图主视图**(非 tab):顶栏房子下拉 + 搜索框 + 「列表」「编辑」;编辑态左侧酷家乐式侧栏(房间/家具/库子 tab);左下角楼层切换器(`N`F,>1 层显示,点击切层);空态画布中央大「+」建房子。
- **加数据两方式并存**:「列表」= 旧 CRUD(房子/房间/家具/物品,不画图也能用);画布内 = 拖矩形/逐点描绘画房间后弹窗命名,家具从侧栏「家具」tab 一键摆到画布(默认 200,200)、「库」tab(room_id 空)拖回房间。
- **渲染**:SVG 单层,房间 `<polygon>`、家具 `<rect>`(右下角缩放手柄)、物品 `<circle>`、底图 `<image>`;`transform: translate() scale()` 平移缩放;fit 自动适配一屏。
- **搜索定位**:搜索走 `GET /item/list?keyword=`(全字段),命中结果右下角列表(房子/房间/家具/位置分组),点条目切房/楼层 + 画布高亮(`highlightItemIds` 变暖黄)。
- **后端接口**(已实现,见 §10):`GET /item/floor-plan`、`PUT /item/house/{id}/floor-plans`、`PUT /item/room/{id}/geometry`、`PUT /item/furniture/{id}/geometry`、`PUT /item/{id}/place`,均带 `@OperationLog`。
- **未实现(留待 S3/S4/S5)**:边对边/顶点对顶点吸附(仅做了顶点轴对齐吸附)、对齐虚线、尺寸标注/数值输入、底图标定、房间面积、家具随房间级联平移、手绘风视觉、移动端只读户型图、撤销/重叠提示/小地图等。

## 15. 实现记录二(S3~S5 + 测试迭代 + 裁剪/粘合,2026-09-04)

> S3 吸附、S4 尺寸标定、S5 打磨、四轮用户测试反馈驱动的编辑手势与页面迭代、房间裁剪(拆分)/粘合(合并),全部完成于 item_seeker 分支(11043ce..21326f0)。

### 15.1 S3 吸附(实装形态)

- **顶点磁吸**:拖房间主体/拖边/拖顶点时,顶点靠近其他房间顶点 6px 内吸附,绿色对齐虚线提示;拖顶点吸附瞬间手柄变绿。
- **家具贴墙**:拖家具时家具边贴合房间边/其他家具边(水平/垂直,阈值 6px),自动平移修正+对齐虚线。
- **边拖拽对齐**(迭代后最终形态,见 15.4):拖接近水平/垂直的边时拉平+单轴锁定+邻边吸直+对齐其他房间边线,四层联动。

### 15.2 S4 尺寸与底图(实装形态)

- **面积**:房间名下方显示 shoelace 面积换算 m²(按楼层 scale)。
- **边长**:编辑态每条边中点标注米数;双击边手柄弹框数值输入(保持边方向缩放对端点,松手持久化)。
- **底图**:支持图片与 PDF(pdfjs-dist 渲染第一页为 PNG 再上传,独立异步 chunk 仅 PDF 时加载,不影响入口体积);`pointer-events:none` 纯背景不挡画布交互;不透明度滑杆 0.1~1 按楼层存 `floorPlans[层].opacity`(保存后回写本地 houses,否则切层回来 watch 解析旧值导致回退)。
- **标定**:标定工具在底图上点两点(橙色圆点+虚线),输入真实距离(米)换算该楼层 scale;悬停按钮 tooltip 解释含义。
- **手绘风**:feTurbulence+feDisplacementMap SVG 滤镜(`#fp-rough`),**仅查看态应用**(编辑态直线直角便于对齐)。
- **移动端**:只读(CSS 隐藏编辑入口)+ 双指 pinch 缩放画布(pointer capture 追踪两指,以中点为缩放中心)。

### 15.3 S5 打磨(实装形态)

- **撤销 Ctrl+Z**(栈 50,编辑态):房间用 lastRoomGeom 链式记录(连续拖拽逐步回退);家具靠副本隔离(placedFurnitures 为 ref 深响应式副本,原数据保留旧值);物品拖拽携带 prevRel 回传。
- **房间复制**:侧栏房间列表「复制」按钮(偏移 40px+「副本」后缀;**从画布双击挪走**——双击名称改名与双击房间复制手势冲突,复制挪到侧栏后画布双击无副作用)。
- **重叠警示**:编辑态房间多边形相交红描边;判定=严格内部+边严格交叉(顶点在对方边上不算),相邻共边/角对角不误报。
- **空楼层引导**:有房子但当前层无房间时,画布中央提示「画下第一个房间」点击进编辑。
- 跳过项(YAGNI):乐观锁(家庭低并发)、小地图(户型图小 fit 够用)、网格吸附(已有三层吸附)。

### 15.4 编辑手势迭代(四轮用户测试反馈驱动)

- **直角顶点「角缩放」**:拖动开始时判定角形态(整次拖拽固定)——轴对齐直角默认角缩放:顶点自由移动,水平邻边对端跟随 dy(边垂直平移)、垂直邻边对端跟随 dx(边水平平移),始终保持直角(PS 自由变换同款语义);**按住 Ctrl=只动该端点**(无任何吸附,可拉斜边);非直角=自由拖+轴对齐吸附辅助调直+磁吸。
- **家具 8 向缩放**:4 角+4 边中点手柄(角=nwse/nesw 光标、边=ns/ew 光标;拖过对角防反转,min 20px),替代原单右下手柄。
- **家具拖拽过程效果**:家具副本从 computed 返回值改为 ref+watch 深响应式(computed 返回的普通对象非响应式,拖拽修改不触发渲染,松手才跳位)。
- **家具级联平移**:拖房间时归属家具跟随(拖前快照 furnOrig,松手逐个保存带 prev 供撤销)。**关键坑**:拖家具保存后必须 `Object.assign` 回写本地原对象——否则下次级联平移的基准是拖之前的旧值,家具跳回原位。
- **边拖拽四层逻辑**(基于**原始边**判定,平移不改变边方向、实时判定永远不触发):①原始边斜率<25% 视为接近水平/垂直,拖拽时两端拉平;②轻微正交位移吸附归零(物理 8px/k 内,纯单轴移动,邻边保持垂直/水平);③邻边吸直 straightenNeighbor(邻边斜率<25% 时自由端对齐拖动端点的 x/y);④对齐其他房间边线 findAlignLine(共线阈值按视图缩放换算)。
- **工具分发 routeTool**:画图/标定/裁剪/粘合工具下,点击任何元素(房间/家具/物品/手柄)都落到画布层不被吞点;切工具清 hover 残留;非 select 工具不启动元素拖拽。
- **逐点描绘修复**:pointerup 不清描绘状态(此前拖拽结束把状态清了,永远只能画 1 个点);双击闭合(去重阈值按视图缩放换算,缩放视图下不再多点);预览多边形/吸附线/标定点等装饰元素统一 `pointer-events:none`(预览线始终连到鼠标位置,曾自己挡住落点)。
- **画图/标定工具十字光标**;**拖拽中不 fit**(级联平移改 props 触发 deep watch→fit 重置视图,视觉上其他房间"反向移动");**ResizeObserver** 监听画布尺寸变化重新 fit(侧栏出现/窗口缩放自适应,拖拽中保护)。

### 15.5 页面与侧栏迭代

- **画板全宽**:破 `.page` 1100px 限制(页面级例外),高度 `calc(100vh-70px)` 底部留距。
- **家具预设拖入**:家具 tab 8 种预设(衣柜/床/书桌/餐桌/沙发/茶几/冰箱/柜子,按真实比例的形状缩略图),HTML5 拖入画布:落点找包含多边形的房间自动关联、命名 `<类型><序号>`(同类型递增)、落点居中、addFurniture 直接带 x/y/w/h 创建。
- **侧栏**:房间 tab 加房间列表(复制/删除);家具 tab 预设网格;库 tab 行排版修复(名称 ellipsis+按钮收紧)。
- **楼层入口**(修复死锁):切换器编辑态常显+「+」加层按钮;floors 常含 currentFloor(新开的空层切走后不消失);画图创建房间弹窗楼层字段不再隐藏,保存后自动切到目标层。原死锁:开第 2 层依赖楼层字段,而楼层字段被隐藏、切换器又要已有 2 层才显示。
- **双击改名**:房间名/家具名 text 双击弹框(带原值全量更新——后端 note 无条件覆盖需带原值)。
- **右键删除**:编辑态房间/家具 contextmenu 确认删除(房间删除家具进库、家具绑物品禁删)。

### 15.6 房间裁剪(拆分)与粘合(合并)

- **裁剪**:侧栏「裁剪」工具 → 鼠标在房间边显示**张开剪刀**(两刃 V 形+双环手柄)→ 点击下刀 → 虚线端点随鼠标(暗红),落在**同房间另一条边**上且连线合法时变绿+**闭合剪刀**(两刃收拢)+边投影吸附 → 点击完成拆分。**合法性**(虚线绿的条件):连线不穿过任何顶点、不与任何边严格相交、中点在多边形内(防凹形外绕)——保证最多一拆二、不跨房间。拆分产生「原名1/原名2」(同名序号自动递进 nextRoomName),家具按中心点分配到所在新房间,散放物品按绝对位置分配并重算相对坐标。
- **粘合**:侧栏「粘合」工具 → 鼠标在与其他房间边贴合(共线重叠 segOverlap)的边上显示**牙膏筒**图标(圆角筒身+盖)→ 点击起点 → 虚线终点吸附到共享边段时变绿 → 点击合并为一个,命名「房间A-房间B」;B 房间家具归 A、散放物品按合并后包围盒重算 relX。
- **几何实现**:共享模块 `utils/floorPlanGeom.js`(pointInPoly/onSegment/segsIntersect/splitPoly/mergePolys/segOverlap/cutSegmentValid/polyBBox),画布(渲染判定)与页面(执行)共用;mergePolys 顶点分裂法(A 从 t 绕行到 s + B 从 s 对应点绕行到 t,连续去重+首尾去重);Esc/切工具取消进行中状态(expose cancelPending)。
- 裁剪/粘合不进撤销栈(多实体创建删除,超出单实体几何恢复模型)。

### 15.7 自测基线

- 几何算法 node 自检:重叠检测 9/9、家具缩放/边对齐 9/9、邻边吸直 6/6、裁剪粘合 24/24(矩形垂直切/切角/索引交换/穿顶点拒绝/凹形穿越拒绝/部分贴合合并/无共享边拒绝/首尾去重)。
- API 冒烟:全链路 31/31、预设路径 14/14(带坐标创建/序号/改名保留几何/opacity 持久化)、裁剪粘合调用链 14/14(序号递进/家具物品迁移/删原房间),测试数据自动清理。
- build 通过(pdfjs-dist 独立异步 chunk 483KB+worker 1.2MB,入口 223KB 基线内)。

## 16. 实现记录三(裁剪几何增强 + 编辑交互/底图工具/端点识别/并行化,2026-09-04~05)

> d6ea47b..6531cdc 五连提交,均为前端户型图编辑打磨:裁剪几何补强、编辑交互升级、新增调整底图工具、端点识别增强、裁剪/粘合接口并行化。**纯前端变更,无 schema/接口变更,无 live DB 迁移**。

### 16.1 裁剪几何增强(d6ea47b)

- **切点可落顶点**:`cutSegmentValid` 放宽——切点 p1/p2 可恰为多边形顶点(切角/对角切),但连线内部仍不得穿过任何顶点、不与任何边严格相交、中点在多边形内(防凹形外绕);`samePt(p1,p2)` 直接拒绝。
- **`dedupSeq` 连续重合顶点去重**:切点恰为顶点时,`splitPoly` 拼接处会产生重复点,连续重复点与首尾重复点一并去重。
- **`cutPlanValid` 完整方案校验**:连线合法 + 切完两侧各 ≥3 顶点,拒绝退化切法(切点落顶点、另一切点又在该顶点邻边上产生的 2 点残片/沿线虚切)。

### 16.2 编辑交互升级(5fb21cc)

- **手柄按需显隐**:顶点手柄=鼠标移入房间内部(或正在拖该房间)才显示;边中点手柄=鼠标贴近那条边(或正在拖该边)才显示;画图/标定工具下整体隐藏,尺寸标注不受影响。
- **裁剪/粘合可在手柄下刀**:`detectBoundary` 候选池 + 归属优先级(端点优先、边投影、不吸边中点手柄);重合端点/共享墙/T 形交界按裁剪房间归属;起点跨房间由组件重锚机制切换目标房间。
- **粘合清除共线点**:mergePolys 合并后清除剪切产生的共线点。
- **侧栏工具按钮图标化**:画矩形/逐点描绘/裁剪(剪刀)/粘合(胶水瓶)/上传底图/标定,全部内联 SVG 图标 36px 网格排布。

### 16.3 快捷编辑 + 缩放恒定 + 共享墙角联动(a442d2a)

- **房间/家具名与边长标注点击快捷编辑**:修复 pointer-events 拦截,名称/边长标注可点击直接编辑。
- **手柄及画布内按钮缩放恒定**:加号/剪刀/牙膏筒等画布内手柄与按钮按 `view.k` 反缩放,屏幕尺寸恒定。
- **共享墙角联动拖拽与批量撤销**:拖动共享墙角时,与其他房间重合的顶点一起挪(墙体保持相连),走 `onSaveRoomsBatch` 批量保存,撤销整体回退。
- **侧栏房间复制/删除按钮图标化**。

### 16.4 调整底图工具 + 适配视图 + 光标体系 + 字号恒定(4aa2af0)

- **调整底图工具(image)**:有底图时侧栏新增「调整底图」——拖动平移、四角等比缩放,把底图分辨率与画布比例尺对齐;变换持久化 `floorPlans[层].img = {x,y,k}`(保存后回写本地防切层回退)。
- **适配视图 + fitKey 视口稳定**:画布右上「适配视图」按钮一键回全景;`fitKey` 自增只在换房/换层/换底图触发,**撤销/几何保存不再重置视口**。
- **边长编辑生效修复**:`onEditEdge` 端点回写本地 geometry(此前只改内存未持久化导致失效)+ 共享墙角联动(被移动端点与其他房间重合顶点一起挪,`onSaveRoomsBatch`)。
- **编辑态光标体系**:默认箭头 / 拖动 move / 端点四向箭头(十字中心即热点)。
- **屏幕字号恒定**:房间名与边长标注字号不随画布缩放变化。
- **裁剪/粘合右键取消起点**:右键取消已落起点;**移除房间右键删除**(与裁剪/粘合右键取消手势冲突)。
- **页面贴底 + 侧栏按钮收窄**:画板贴底,侧栏工具按钮改 36px 图标。
- **修预设缩略形状塌陷**:`presetShape` 按 w/h 等比缩进 26px 框(此前引用未定义 pw/ph 导致形状不可见)。

### 16.5 端点识别增强 + 接口并行化(6531cdc)

- **多边形外贴近端点即显手柄**:`roomHandles` 在房间外但贴近某端点(屏幕 12px)时只亮该端点(尖角即现即抓)。
- **透明命中区扩至 12px**:端点双层结构——透明命中圆 `r=12px`(屏幕)在下扩大可点区,可见手柄 `r=6px` 在上保留吸附态与 hover。
- **识别阈值屏幕恒定**:`detectBoundary` 支持 `opts.th`,hover/让位区 `TH = 12/view.k`,缩放视图下识别一致。
- **裁剪/粘合接口并行化**:创建两新房间、几何保存、家具/物品迁移全部 `Promise.all` 并行;**删原房间保持最后**(后端删房会把家具移入库,若先删则迁移被覆盖)。
