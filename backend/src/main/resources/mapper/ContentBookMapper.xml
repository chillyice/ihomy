<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ihomy.mapper.ContentBookMapper">

    <update id="incrViewCount">
        UPDATE content_book SET view_count = view_count + 1 WHERE id = #{id}
    </update>

    <select id="selectCategoriesByFamily" resultType="java.lang.String">
        SELECT DISTINCT category FROM content_book
        WHERE family_id = #{familyId} AND category IS NOT NULL AND category &lt;&gt; '' AND deleted = 0
        ORDER BY category
    </select>

    <update id="renameCategory">
        UPDATE content_book SET category = #{newName}
        WHERE family_id = #{familyId} AND category = #{oldName} AND deleted = 0
    </update>

    <update id="clearCategory">
        UPDATE content_book SET category = NULL
        WHERE family_id = #{familyId} AND category = #{category} AND deleted = 0
    </update>

    <delete id="deletePhysicalById">
        DELETE FROM content_book WHERE id = #{id}
    </delete>

</mapper>
