<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ihomy.mapper.ContentMindmapSnapshotMapper">

    <select id="selectListByMindmapId" resultType="com.ihomy.entity.ContentMindmapSnapshot">
        SELECT id, mindmap_id, family_id, user_id, title, source, created_at
        FROM content_mindmap_snapshot
        WHERE mindmap_id = #{mindmapId}
        ORDER BY created_at DESC, id DESC
    </select>

    <delete id="deleteKeepRecent">
        DELETE FROM content_mindmap_snapshot
        WHERE mindmap_id = #{mindmapId}
          AND id NOT IN (
            SELECT id FROM (
              SELECT id FROM content_mindmap_snapshot
              WHERE mindmap_id = #{mindmapId}
              ORDER BY created_at DESC, id DESC
              LIMIT #{keep}
            ) t
          )
    </delete>
</mapper>
